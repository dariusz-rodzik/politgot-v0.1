import React, { Component } from "react";

export default class AnswerQuestions extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return <p>You will be asked some questions ;)</p>
    }
}
